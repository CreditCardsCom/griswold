@layout('layout.app')

@section('content')
  @set('title', 'View Office')

  <h1>{{ office.name }}</h1>

  <form method="POST" action="{{ route('OfficeController.destroy', { id: office.id }) }}?_method=DELETE">
    {{ csrfField() }}

    <button type="submit">
      @!component('icon.trash', { size: 40 })
    </button>
  </form>
@endsection
